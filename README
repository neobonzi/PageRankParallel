To compile all programs follow these command line instructions:
$ make
This will generate two binaries: one named micrank, the other named cudarank.

To run the micrank binary, use the following command:
$ ./micrank <path to file that contains the data set>

To run the cudarank binary, use the following command:
$ ./cudarank <path to file that contains the data set>
